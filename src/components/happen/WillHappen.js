import React, { Component } from 'react';
import Layout from './../Layout';

export default class WillHappen extends Component {
    state = { }

    render() {
      return (
        <Layout>
          <h2>Will Happen</h2>

        </Layout>
      );
    }
}
