import React, { Component } from 'react';
import Layout from './../Layout';

export default class HasHappened extends Component {
    state = { }

    render() {
      return (
        <Layout>
          <h2>Has Happened</h2>

        </Layout>
      );
    }
}
