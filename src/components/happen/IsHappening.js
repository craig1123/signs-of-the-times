import React, { Component } from 'react';
import Layout from './../Layout';

export default class IsHappening extends Component {
    state = { }

    render() {
      return (
        <Layout>
          <h2>Signs That Are Happening</h2>

        </Layout>
      );
    }
}
